$("#feedback").html("<%= escape_javascript(render('form', :feedback => @feedback)) %>");

$.fancybox({
        padding: 0,
         maxWidth       : 720,
        maxHeight       : 290,
        fitToView       : false,
        width           : '100%',
        height          : '100%',
        autoSize        : false,
        closeClick      : false,
        openEffect      : 'none',
        closeEffect     : 'none',
        'href'                  : '#feedback',
         helpers        :         {
        overlay                      : {
            opacity                 : 0.8,
            css                     : {
                'background-color'  : '#FFF'
            }
        }
    } // end helper
});